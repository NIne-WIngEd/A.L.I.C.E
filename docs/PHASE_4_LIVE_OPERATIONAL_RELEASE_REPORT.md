# Phase 4 P4.10 Operational Live-Public-Information Release Report

## Status

P4.10 is an additive successor profile to the approved P4.0–P4.9 fixture-governed compatibility release.

Public implementation is complete through:

- P4.10a — exact Brave live-search provider and controlled public-page fetch foundation;
- P4.10b — one explicit foreground PUBLIC research path through injection, temporal/freshness, exact grounding, Phase 3 projection, P3.6, and P4.5b;
- P4.10c — private exact-commit acceptance tooling and tamper-evident release record.

Phase 4 is operationally closed only when the private P4.10c audit returns `approved=true` for the exact clean P4.10c feature commit and that unchanged tree is merged into `main`.

## Exact live path

`research mode → Brave Search API → LiveControlledInformationHttpRetriever → injection firewall → temporal metadata → freshness analysis → exact extractive claims → Phase 3 grounding projection → P3.6 pre-commit hook → P4.5b citation validation`

## Release evidence

The P4.10 targeted surface is pinned at exactly 101 passing tests with zero skips.

The private record is written outside Git at:

`C:\ALICE_Vault\reports\phase4-live-information-acceptance.json`

It binds the release commit and rollback ancestor, all deterministic P4.10 tests, the full repository regression, provider and research policy digests, benchmark digest, repository snapshots, live egress receipts, live research receipt, source dispositions, exact validation digest, and approval decision. It stores no raw query, source body, API credential, provider response body, or model prompt. The release floor requires at least 2,124 repository tests passed, no more than the two previously approved skips, and all 14 subtests passed.

## Preserved profiles

The exact P4.1 fixture registry and P4.6a, P4.7a, P4.7b, P4.8, and P4.9 policies remain compatibility artifacts. P4.10 does not change their fixture-only booleans or canonical digests.

## Inactive capabilities

This release does not activate source-body persistence, Phase 5 storage, memory writes, external actions, recursive browsing, retries, provider fallback, authenticated page browsing, or background execution. These are bounded profile exclusions, not permanent capability ceilings.

## Phase boundary

Phase 5 remains blocked until the P4.10c private record is approved and the exact audited tree is merged. After that merge, Phase 4 is both fixture-released and operationally live for the one accepted PUBLIC research path.
