# MC10D v1.7 — Exhaustion-Aware Repair Boundary

## Why v1.7 exists

MC10D v1.6 solved the Kaggle transport chain and reached the real 64-slot repair worker. The worker produced 62 independently valid replacements, then the frozen v1.2 repair generator exhausted its full three-prompt-attempt allowance on raw candidate `ASYN-F694986B9D502671BCFC9D58` (`repair_initiation`, target role `peer`). The third attempted replacement still violated deterministic hygiene with `RAYAN_SUBSTITUTED_FOR_PEER`. The v1.2 worker correctly stopped before slot 64.

The frozen repair policy permits **three prompted repair attempts per slot**. v1.7 does not add a fourth attempt and does not weaken the hygiene rule. Instead it applies the existing UNKNOWN/ABSTAIN doctrine conservatively: a slot whose exact three-attempt repair allowance is independently proven exhausted is deferred to that packet's UNKNOWN competitor and remains an open completion frontier. Its hard-blocked original never returns to the effective candidate pool and the slot earns zero saturation credit.

## Bound current frontier

- Total repair obligations: 64.
- Immutable v1.6 checkpoint expected: 62 valid R1 replacements.
- Verified exhausted slot 63: `ASYN-F694986B9D502671BCFC9D58`.
- Exact final blocker: `RAYAN_SUBSTITUTED_FOR_PEER` on repair attempt 3.
- Slot 64, if still unresolved: `ASYN-FC065716EC22BF8D0174E4F2`, `loyalty_protection`, frozen raw blocker `BETRAYAL_DIRECTION_REVERSED`.
- Slot 63 is never regenerated by v1.7.
- Slot 64 is allowed only the unchanged v1.2 `repair.generate` path, which itself has the frozen three-attempt maximum.
- A technical/model/JSON/runtime failure is **not** converted into an abstention.
- If slot 64 also genuinely exhausts all three prompted attempts with deterministic hygiene blockers, it is also deferred rather than forced.

Therefore the effective candidate pool is:

- 287 if slot 64 succeeds: 224 retained originals + 63 valid replacements; one verified exhausted slot.
- 286 if slot 64 also exhausts: 224 retained originals + 62 valid replacements; two verified exhausted slots.

The 24 packet UNKNOWN competitors remain present in either case.

## What does not change

v1.7 does not alter E0, E-INF, A-SYN semantics; the owner-ratified doctrine; the four judge families; the v1.1 deterministic hygiene rules; the v1.2 repair prompt/generation function; the three-attempt cap; the v1.1 scenario functions; hidden-MC8 isolation; the 18 falsification families; the eight zero-tolerance gates; acceptance/promotion authority; or model-training authority.

E0 remains source-person truth authority. E-INF remains uncertain hypothesis. A-SYN remains nonhistorical compatible Alice behavior. Repair exhaustion does not create evidence, a candidate, or saturation credit.

## Dynamic scenario geometry

Every candidate that actually exists in the current effective pool still receives the unchanged 48 packet-shared + 16 candidate-specific tuples and three paraphrases:

- 287 candidates → 4,592 candidate-specific scenarios → upper bound 55,104 candidate-probe obligations.
- 286 candidates → 4,576 candidate-specific scenarios → upper bound 54,912 candidate-probe obligations.

The 1,152 packet-shared scenarios and the 3,584 retained-original candidate-specific scenarios remain byte-identical to v1.1. An exhausted slot has no candidate-specific scenarios because it is not a candidate.

## Hidden evaluator firewall

MC8 evaluator-only bytes remain invisible to repair generation and public judge qualification. They are copied only after all 64 repair obligations are resolved by either a valid candidate or verified abstention, the four judges are bound, and the current effective candidate/scenario registry is frozen. Pointwise scope judging still may not open MC8.

## Execution boundary after success

A successful v1.7 run authorizes only the blinded pointwise scope-judge screen for the current effective pool. Full 64×3 simulation/falsification remains blocked until the pointwise screen freezes. Final MC10 saturation, personality-model training, Stage G closure, and production promotion remain blocked because the verified repair exhaustion leaves the completion frontier open.
