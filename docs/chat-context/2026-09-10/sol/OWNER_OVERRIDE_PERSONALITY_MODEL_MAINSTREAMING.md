# Owner override: mainstream the A.L.I.C.E. personality-model build

Date: 2026-09-10

Source continuation export: `Review-Git-Repository-Context (1).pdf`

Source PDF SHA-256: `34CEC5C8329F5A91158F93B32747F8E91DF169B0392CC5CADE5A14B6A5491703`

Canonical `main` remains frozen at `0abaed85873c3f8de04765847eb7700b0e20433f`.

This note is a continuity/governance handoff only. It does not mutate canonical `main`, promote synthetic material, or create model weights.

## Superseding owner decision

The owner explicitly superseded the validation-heavy MC10D continuation for the current personality-model build path.

The working goal is now direct scientific/product progress toward the most Elaina-faithful A.L.I.C.E. personality weights:

1. raw Elaina source material -> canonical `E0`;
2. structured claims / episodes / relations from E0;
3. hypotheses for missing personality coverage;
4. `E-INF` derived from the evidence and explicit hypotheses;
5. `A-SYN` synthetic reconstruction / behavioral training material;
6. scenario/simulation-style generation where it improves training coverage;
7. assemble the actual personality-training corpus;
8. perform exactly one final Astra review of the completed data design + weight-creation architecture immediately before any model-weight creation;
9. fix only material issues found by that review;
10. train the Elaina Identity / Personality Model;
11. owner performs the decisive personality-fidelity judgment because the owner has the strongest direct familiarity with Elaina.

The earlier multi-judge, multi-family, packet-by-packet validation tournament is no longer a prerequisite for this build path. Existing MC10D evidence is preserved as historical/provenance material rather than erased, but it must not keep the personality-model work blocked unless the owner explicitly reopens that workflow.

## What remains non-negotiable

The owner removed validation bureaucracy, not provenance or engineering discipline.

Preserve:

- explicit provenance classes (`E0`, `E-INF`, `A-SYN`, and later experiential/self provenance as applicable);
- the distinction between observed Elaina evidence, inference, and synthetic reconstruction;
- immutable/content-addressed source identities where practical;
- reproducible transforms and training-corpus lineage;
- durable checkpoints;
- technical failure evidence;
- separation between Elaina personality weights and host-specific / continuously changing memory;
- one final Astra architecture/data review immediately before weight creation.

Do not silently promote inference or synthetic material into E0.

## Streamlined Stage G interpretation

For this owner-directed path, Stage G is broader than the personality model alone.

### G1 — Elaina identity/personality foundation

Raw Elaina material -> E0 -> structured evidence -> E-INF -> A-SYN -> training corpus -> final Astra pre-weight review -> Elaina Identity / Personality Model weights.

### G2 — Memory Formation Model

Build the separate learned component that converts experiences into candidate memories. This is distinct from the Elaina personality model.

### G3 — Rayan Host Model + relationship state

Build the host model from evidence-linked host memory. Host data may update A.L.I.C.E.'s understanding of Rayan and the relationship state. It must not rewrite the Elaina-derived personality anchor.

### G4 — End-to-end synthetic host-life integration

Generate a substantial fictional/synthetic host-life stream and pass it through the intended memory architecture:

`Experience Fabric/Ledger -> Memory Formation -> authority/claim layer -> graph/vector/episodes -> Host Model -> relationship model -> retrieval/context -> A.L.I.C.E.`

The synthetic host-life stream must remain marked synthetic/fictional and must not silently become the owner's real biography.

The practical Stage G close condition is that the personality model, Memory Formation Model, host/relationship modeling, memory fabric, and synthetic host-life behavior work together as one system capable of the behaviors claimed for A.L.I.C.E.

## G+ and Phase 2 transition

Treat G+ as integration/refinement: intelligent recollection, attention, selective context assembly, and making the complete system behave like one mind rather than a database attached to a model.

The previous Stage H -> I -> J bureaucracy is not the active owner-directed prerequisite for replacing Phase 2. After Stage G + G+ demonstrate the intended system, the owner may ratify the new architecture, preserve Phase 2 temporarily as rollback/archive, and make the cognitive-memory fabric canonical.

## Last MC10D execution evidence before the override

The final useful R2 Kaggle evidence should be retained only as historical execution context:

- Kaggle 2xT4 route was finally shown to execute the repaired Mistral path successfully;
- public context/schema probe passed;
- one private Mistral candidate completed scenario batch 1, scenario batch 2, and falsification validly;
- the run demonstrated that the repaired execution route was real but also exposed that brute-force four-family coverage would be prohibitively slow.

This is not the next work item under the owner override.

## Error history that must not be relearned

Carry these implementation lessons forward even though the validation workflow is superseded:

- do not build giant PowerShell state machines when a smaller controller is safer;
- never mix Windows and POSIX path semantics;
- never treat a failed scheduler/provider query as proof that a job does not exist;
- never blind-resubmit an ambiguous remote identity;
- preserve failure evidence before retrying;
- do not silently overwrite immutable artifacts or hashes;
- avoid UTF-8 BOM regressions in Kaggle transport;
- make checkpoints content-addressed and verify durable round trips;
- distinguish actual provider runtime from conservative synthetic quota accounting;
- do read-only/local forensic diagnosis before spending scarce GPU time when a failure boundary is unclear;
- technical failures must not be converted into scientific/personality judgments;
- tests must cross real binding/serialization boundaries rather than merely asserting constants.

## Current boundary / next action

Do **not** resume the old MC10D validation queue by default.

The next task is architectural research and design for weight creation itself. Before generating another large synthetic corpus or launching training, determine the strongest current approach for an Elaina-faithful personality model using:

- current frontier/open-model post-training practice;
- personality/role/behavior fine-tuning research;
- SFT vs continued pretraining vs parameter-efficient tuning;
- LoRA/QLoRA/DoRA and related adapter methods;
- preference optimization / contrastive objectives;
- synthetic-data generation and hard-negative construction;
- provenance-aware sampling/curriculum;
- catastrophic-forgetting/identity-drift controls;
- modular separation of personality weights from host memory and relationship state;
- actual available compute/storage and deployment constraints.

The research output must answer: what we already have, what data is still missing, what model-weight architecture should be used, how the corpus should be constructed, which base-model family is appropriate, how training should be staged, and what exactly Astra receives for the single final pre-weight review.

No model training has been authorized by this continuity note itself. Training begins only after the data/architecture plan is grounded and the owner proceeds through the single final Astra review boundary.
