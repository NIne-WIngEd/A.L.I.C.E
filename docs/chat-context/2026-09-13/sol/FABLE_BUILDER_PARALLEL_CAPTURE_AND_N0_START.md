# Fable Builder Parallel Capture + N0 Start

**Date:** 2026-09-13

Owner clarified that the future Fable Builder Model (FBM) does not need to internalize A.L.I.C.E.-specific provenance labels such as E0, E-INF, A-SYN, or UNKNOWN as product identity concepts. It must learn the general operations that correctly generate the corresponding evidence, hypothesis, unresolved, and synthetic-behavior classes from consumer-authorized data.

Active rule from this point forward:

- continue building A.L.I.C.E. normally through N0, N1, N2, N3, and later runtime formation;
- after every material build step, save one compact reusable process trace on `fable-builder-model`;
- process traces capture inputs/references, procedure, constraints, decision criteria, outputs, validation result, owner feedback, and reusable lessons;
- traces are not hidden chain-of-thought archives and do not place private Elaina payloads on the public branch;
- A.L.I.C.E. remains the primary build. FBM capture must not become another MC10-style project or block Alice progress.

Historical raw-data enrichment methodology has been captured on `fable-builder-model` in `RAW_DATA_TO_PERSONALITY_SUBSTRATE_PLAYBOOK_v0.1.md` plus a retrospective trace set covering evidence decomposition, hypothesis generation, UNKNOWN preservation, synthetic behavior generation, curation, and targeted regeneration.

N0A has now started on `alice-eipm-v1-build` with a concrete first native model decision:

- `alice-n0-semantic-v0.1`;
- native bidirectional transformer encoder;
- 24 layers;
- hidden size 896;
- 14 attention heads;
- FFN 3584;
- GeGLU, pre-norm, RoPE;
- alternating local/global attention;
- initial max train length 4096;
- 48k public-only byte-fallback BPE tokenizer;
- approximate initial scale ~350M parameters;
- public-corpus MLM foundation plus Sol-authored targeted semantic/pragmatic/social/judgment curriculum;
- no fixed scale ceiling;
- no teacher/judge tournament gate;
- first private identity gradient still requires owner authorization.

The N0 start itself was mirrored as an FBM trace. This establishes the intended dual workflow: each real A.L.I.C.E. construction step simultaneously expands the future native Fable personality-builder training corpus.
