# Voice Overlay v0.2 + N0 v0.5 Coverage Boundary

## Closed results

The N0 v0.4b voice-first teacher-bank audit passed with 383 unique rows, 51 competencies, no failures, and no private identity data/gradient.

The private voice-identity substrate was then reviewed for actual saturation rather than raw row count. Curated Frontier v2 already contains 32 selected voice-relevant E0 anchors, 77 curated E-INF rows supported by those anchors, and 542 voice/communication-relevant curated A-SYN candidates.

Voice overlay v0.1 contained only 40 structured delivery-policy A-SYN rows and was uneven across the eight N0 voice competencies. A coverage-driven v0.2 expansion added 56 targeted A-SYN rows, producing 96 structured voice-policy rows total. Each VOICE-01..VOICE-08 competency now has 12 structured identity-policy cases. All 32 selected E0 voice anchors are represented by at least one structured row and 52/77 curated E-INF voice anchors are used directly.

No new E-INF was generated. Further identity-specific voice synthesis is failure-driven from owner review or N1/N2 evaluation rather than quota-driven.

Private gradients remain unauthorized.

## Next public N0 teacher-bank boundary

The active curriculum contract requires at least 15 independent train and 5 independent dev scenarios per each of the 51 public competencies before the full multitask floor is considered satisfied.

The PASS v0.4b bank has 383 rows. The exact deficits to reach 15 train + 5 dev everywhere total **637 rows**:

- 490 additional train rows;
- 147 additional dev rows;
- final governed bank size: 1020 rows.

This is a better target than simply adding the previously reported 617 rows to cross the numerical 1000-row floor, because it satisfies the per-competency coverage rule instead of padding the bank.

Sol is the authorized direct generator for this public coverage wave. No external model/agent is needed. The wave remains public/non-identity and must not ingest Elaina/Rayan/private substrate.

## Operating rule

Voice identity is now sufficiently covered for the current pre-weight stage. Do not restart broad voice synthesis. Continue the public N0 build, then use actual N1/N2 spoken-fidelity failures to request targeted additional A-SYN if needed.
