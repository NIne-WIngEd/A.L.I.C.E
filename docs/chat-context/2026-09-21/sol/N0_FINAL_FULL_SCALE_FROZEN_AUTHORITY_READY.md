# N0 final full-scale frozen-authority closure — ready for one owner Magnolia run

**Date:** 2026-09-21  
**Status:** final pre-launch package completed; exact-head static/CPU qualification passed; exact-source Graphify passed; private operational history reconciled; Fable updated; one owner Magnolia run is the next scientific evidence  
**Implementation branch:** `alice-eipm-v1-n0-frozen-semantic-authority-v3`  
**Exact qualified HEAD:** `0a8ac74aa6fa73259c03dc4d8754a42a6fde2307`  
**GitHub Actions:** `35617418279` — SUCCESS  
**Graphify exact-source audit:** `35617626581` — SUCCESS, Graphify 0.9.63  
**Graphify result branch HEAD:** `6e86631eb47882574f80aa505820f2c93f933fac`  
**Fable branch HEAD:** `ad4afdaee87726787efc9cbcaf7a8664553628d4`

N0 is still incomplete. This handoff authorizes exactly one owner-submitted Magnolia closure run. It does not claim a model pass.

## Valid failure chain

The current architecture is based on preserved model evidence, not a tuning chain.

- Job 575956: Production P2-v1 fit TRAIN but failed relation program transfer.
- Job 575957: zero-gradient localization showed active-mask exactness about 0.952 while relation identity collapsed with path length. Ordered relation identity was primary. Survival was not.
- Job 575958: Production-v3 full P2 still failed. The architecture allowed an implicit closed semantic metric and a branch recomposition had lost explicit ordered evidence consumption.
- Job 575962: P2S-v1 failed the broad semantic generalization gate before Production P2.
- Job 575966: P2S-v2 repaired schema grammar, factor supervision and pair interaction. Production-core fitting reached 1.0, but unseen relation and held-out factor semantics stayed below gate. A learned P2S matcher was therefore rejected as semantic authority.

No failed model experiment is rerun unchanged. No LR, step, width, batch or threshold search is authorized.

## Final semantic authority

P2A now tests a zero-gradient semantic authority before Production P2.

It uses four already-trained/frozen N0 surfaces:

1. joint prompt/candidate preference scoring;
2. semantic-projection cosine;
3. semantic-to-rationale principle alignment, including the trained rationale projection;
4. parameter-free token evidence.

The four surfaces are candidate-axis normalized only after the active runtime candidate subset is selected.

Production TRAIN receives only the six core relation meanings. Open-schema and final-only relations do not enter TRAIN gradient and do not act as negatives through the normalization denominator.

Production DEV receives its complete runtime schema. The native final gate receives its frozen final runtime schema.

There is no semantic-authority optimizer, learned relation-ID table, learned factor-class identity head, candidate-count parameter axis or hop-count parameter axis.

If P2A fails, Production P2 remains closed. That failure means the ratified N0 semantic base itself does not provide enough zero-shot schema semantics for closure. Do not patch P2S or lower the gate.

## Ordered operator and P1 interface

The Production operator retains the causal lesson from job 575957.

It carries explicit remaining-query-evidence coverage across recurrent relation steps. The relation distribution remains continuous. Frozen semantic authority determines relation identity while remaining evidence and a bounded recurrent residual determine program order.

STOP and UNKNOWN remain separate from relation identity.

P1 relation state is deliberately **not** relation-matching authority and **not** Binder support authority. It is still preserved as the proven executor coordinate/interface. P2 retains nonzero continuous-state alignment into that executor space.

## Binder and structural support

Binder v2 is the first exact sparse boundary. The operator does not use exact sparse relation selection. Binder v2 owns adaptive exact-zero structural support. No fixed top-k support is introduced.

The same recurrent operator weights can run beyond the current training fixture step count. The current step budget is an experiment operating point, not a serving capability ceiling.

## Full-scale N0 parent stack

The package is not a pilot. The final native gate consumes the ratified semantic checkpoint, structured state, evidence adapter, selected repaired DualEndpoint graph, ratified fusion, adaptive multi-view latent pool, corrected P1 executor, Production P2 operator, Binder v2 and the frozen native validation corpus.

Major parent artifacts are hash-checked before any new evidence root is created.

### Repaired evidence-parent reconciliation

The final audit found one stale inherited path in the closure runner. It still pointed at the older canonical:

`relation-repair-v0.1/step-00000080/evidence_graph_dual_endpoint.safetensors`

SHA `ec9942bd71a39c8552804322f1a36b7a6c5f009446e56423c80e59479ad8fa79`.

Private Magnolia history and the public causal-arbitration documents showed that the later endpoint repair had already been the stronger parent used by subsequent N0 relation/query work:

`relation-endpoint-repair-v0.2/training/step-00000200/evidence_graph_dual_endpoint.safetensors`

SHA `3ae08aa2fc2c46ed74a47792310c6c2bf202fad800549cecc36c5365523dc47f`.

Earlier full-stack arbitration classified that candidate as IMPROVEMENT versus the older canonical graph, with no frozen arbitration metric degradation.

Historical governance is preserved: that arbitration did **not** call the candidate production-promoted. The new full-scale closure package preselects it explicitly as its base evidence parent rather than rewriting the old decision.

This does not rebuild the frozen final corpus, schema, manifest or thresholds. The evidence graph is a model input supplied at evaluation time. The final evaluator now enforces and records the repaired graph SHA.

## Anti-loop runner hardening

The runner previously created the immutable evidence root before completing all read-only source/artifact preflight checks. A setup defect could therefore poison the run root without producing scientific evidence.

The runner now verifies the exact revision, prior evidence roots, required files, major parent hashes, P1/575958/575966/frozen-final lineage and the full-scale manifest first. Only then does it create the new immutable evidence root.

After that point, a P2A/P2/P3/P4/final failure is genuine governed evidence and must remain preserved.

## Exact qualification

GitHub Actions `35617418279` passed on exact source `0a8ac74aa6fa73259c03dc4d8754a42a6fde2307`.

It verifies source compilation, mechanics, 575966 authority, four-surface zero-gradient semantics, core-only TRAIN candidates, holdout isolation, no trainable relation/factor identity, no-ceiling full-scale contract, repaired graph lineage, unchanged downstream compute/gates, Binder v2 sparse boundary, immutable evidence-root discipline, original frozen final reuse and P3/P4/final authority lineage.

## Exact-source Graphify audit

Graphify rebuilt from the exact worktree at `0a8ac74aa6fa73259c03dc4d8754a42a6fde2307`.

Workflow `35617626581` completed successfully with Graphify 0.9.63.

It sees the final branch as the maximal N0 experiment frontier while retaining the older ordered-evidence and P2-failure-localization branches as separate historical frontiers.

Graphify remained navigation-only. Its private-history recommendation was followed. That private reconciliation exposed the stale old graph path before launch and reconfirmed the ordered-evidence, open-schema-negative, P1-interface and repaired-parent lessons. No additional source-level launch blocker remains.

## Fable continuity

`fable-builder-model@ad4afdaee87726787efc9cbcaf7a8664553628d4`

adds:

`docs/fable-builder/traces/FBM_TRACE_20260921_N0_FINAL_FULL_SCALE_FROZEN_AUTHORITY.jsonl`

It records the fourth semantic surface, repaired-parent reconciliation, anti-loop preflight hardening and exact final qualification. Fable does not claim model success.

## One authorized next action

Use the Magnolia login node and the existing repo. Magnolia has old Git, so use `git checkout`, not `git switch` or `git -C`.

```bash
cd "$HOME/rayan-compute/rayan-eipm-main" || exit 1

BRANCH="alice-eipm-v1-n0-frozen-semantic-authority-v3"
EXPECTED="0a8ac74aa6fa73259c03dc4d8754a42a6fde2307"

if [ -n "$(git status --porcelain --untracked-files=no)" ]; then
  echo "STOP: tracked repo changes exist"
  git status --short --untracked-files=no
  exit 2
fi

git fetch origin "refs/heads/$BRANCH:refs/remotes/origin/$BRANCH"

if git show-ref --verify --quiet "refs/heads/$BRANCH"; then
  git checkout "$BRANCH"
  git pull --ff-only origin "$BRANCH"
else
  git checkout -b "$BRANCH" --track "origin/$BRANCH"
fi

HEAD="$(git rev-parse HEAD)"
echo "HEAD=$HEAD"

test "$HEAD" = "$EXPECTED" || {
  echo "STOP: wrong final N0 frozen-authority HEAD"
  exit 3
}

export ALICE_N0_REPO_ROOT="$PWD"
export ALICE_N0_WORKDIR="$HOME/rayan-compute/rayan-n0/n0-v02"
export ALICE_N0_EXPECTED_REVISION="$HEAD"

test -f "$ALICE_N0_WORKDIR/qsre-n0-closure-semantic-v2/p2s-schema-matcher/result.json" || {
  echo "STOP: preserved job 575966 evidence is missing"
  exit 4
}

if [ -e "$ALICE_N0_WORKDIR/qsre-n0-frozen-semantic-authority-v3" ]; then
  echo "STOP: final evidence root already exists; preserve it"
  echo "$ALICE_N0_WORKDIR/qsre-n0-frozen-semantic-authority-v3"
  exit 5
fi

sbatch scripts/eipm/n0/magnolia_p100_n0_v02_qsre_n0_frozen_semantic_authority_v3.sbatch

squeue -u "$USER"
```

The Slurm script requests one P100, 8 CPUs, 32 GB RAM and six hours. It runs the complete governed chain:

`P2A -> authority caches -> P2 -> P3 -> P4 -> original frozen native N0 final gate`.

There is no owner-side micro-gate between stages. Each stage fail-closes the next.

## Result interpretation

If P2A fails with infrastructure clean, preserve it as a semantic-base capability failure and stop.

If P2 fails, preserve it and localize the first valid operator failure. Do not tune around it.

If P3 fails, preserve Binder evidence and stop.

If P4 fails, preserve integration evidence and stop.

If the final native gate fails, preserve the single frozen result and localize the failing objective block. Do not change thresholds or automatically rerun.

N0 closes only if the unchanged native result is:

`PASS_N0_FINAL_SELF_VALIDATION_OBJECTIVE`

with `n0_complete=true` and `n1_authorized=true`.
